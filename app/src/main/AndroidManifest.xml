<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.jetpack_compose_trainning_005">

    <application
        android:name=".di.TodoApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/pico"
        android:label="Todo_Mvvm"
        android:roundIcon="@mipmap/pico"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jetpack_Compose_trainning_005"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Todo_Mvvm"
            android:theme="@style/Theme.Jetpack_Compose_trainning_005">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>